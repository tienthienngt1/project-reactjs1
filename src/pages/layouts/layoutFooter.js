import { Navbar } from "react-bootstrap";

const LayoutFooter = () => {
    return (
        <div className="mt-5">
            <Navbar bg="dark">
            </Navbar>
        </div>
    );
};

export default LayoutFooter;